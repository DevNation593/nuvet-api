# Forzamos hoisting para mantener compatibilidad con código legacy (NestJS
# resuelve muchos módulos por path relativo). `node-linker=hoisted` se
# removió en pnpm 9+ y emite warning en pnpm 10 — no lo necesitamos
# porque `shamefully-hoist=true` ya cubre el caso de uso.
shamefully-hoist=true
